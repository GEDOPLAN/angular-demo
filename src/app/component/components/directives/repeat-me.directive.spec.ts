import { RepeatMeDirective } from './repeat-me.directive';

describe('RepeatMeDirective', () => {
  it('should create an instance', () => {
    const directive = new RepeatMeDirective();
    expect(directive).toBeTruthy();
  });
});
