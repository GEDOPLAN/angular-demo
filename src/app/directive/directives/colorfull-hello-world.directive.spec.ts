import { ColorfullHelloWorldDirective } from './colorfull-hello-world.directive';

describe('ColorfullHelloWorldDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorfullHelloWorldDirective();
    expect(directive).toBeTruthy();
  });
});
