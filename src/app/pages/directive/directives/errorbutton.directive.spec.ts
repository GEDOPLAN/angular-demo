import { ErrorbuttonDirective } from './errorbutton.directive';

describe('ErrorbuttonDirective', () => {
  it('should create an instance', () => {
    const directive = new ErrorbuttonDirective();
    expect(directive).toBeTruthy();
  });
});
