import { BoxHeaderDirective } from './box-header.directive';

describe('BoxHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new BoxHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
